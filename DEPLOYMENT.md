# Развертывание Smurf To-Do App на Vercel + Supabase

## 🚀 Пошаговая инструкция

### 1. Настройка Supabase

1. **Создайте аккаунт** на [supabase.com](https://supabase.com)
2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите организацию
   - Назовите проект `smurf-todo`
   - Выберите регион ближе к вам
   - Создайте и сохраните пароль базы данных

3. **Создайте таблицу tasks:**
   - Перейдите в "SQL Editor"
   - Вставьте содержимое файла `supabase-schema.sql`
   - Нажмите "Run"

4. **Получите API ключи:**
   - Перейдите в "Settings" → "API"
   - Скопируйте "Project URL" и "anon public"
   - Это ваши `SUPABASE_URL` и `SUPABASE_ANON_KEY`

### 2. Настройка Vercel

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Авторизуйтесь в Vercel:**
   ```bash
   vercel login
   ```

3. **Разверните проект:**
   ```bash
   cd /path/to/splitwise-todo-main
   vercel --prod
   ```

4. **Настройте переменные окружения:**
   ```bash
   vercel env add SUPABASE_URL
   vercel env add SUPABASE_ANON_KEY
   ```
   Вставьте сохраненные значения из Supabase

### 3. Обновление конфигурации

1. **Отредактируйте `supabase-config.js`:**
   ```javascript
   const SUPABASE_CONFIG = {
     url: process.env.SUPABASE_URL || 'https://your-project-id.supabase.co',
     anonKey: process.env.SUPABASE_ANON_KEY || 'your-anon-key-here'
   };
   ```

2. **Замените placeholder значения** на реальные данные из Supabase

### 4. Тестирование

1. **Откройте ваш сайт** по URL от Vercel
2. **Проверьте регистрацию/вход:**
   - Создайте новый аккаунт
   - Войдите в систему
   - Добавьте несколько задач

3. **Проверьте синхронизацию:**
   - Откройте сайт в другом браузере
   - Войдите с тем же аккаунтом
   - Задачи должны синхронизироваться

## 🔧 Возможные проблемы

### Проблема: CORS ошибки
**Решение:** В Supabase перейдите в "Settings" → "API" и добавьте ваш домен в "Additional Redirect URLs"

### Проблема: Аутентификация не работает
**Решение:** Проверьте правильность URL и API ключей в `supabase-config.js`

### Проблема: Данные не сохраняются
**Решение:** Убедитесь что таблица `tasks` создана и RLS политики настроены правильно

## 📱 Функции

- ✅ **Аутентификация пользователей** через email/пароль
- ✅ **Синхронизация** между устройствами
- ✅ **Offline режим** с localStorage как fallback
- ✅ **PWA** поддержка для установки на мобильные
- ✅ **Liquid Glass дизайн** сохранен
- ✅ **Все оригинальные функции** работают

## 🌐 Доступность

После развертывания ваше приложение будет доступно по адресу:
`https://your-app-name.vercel.app`

## 💡 Дополнительные улучшения

1. **Добавьте Google OAuth** для удобного входа
2. **Настройте email подтверждения** в Supabase
3. **Добавьте бэкапы** данных
4. **Настройте кастомный домен** в Vercel

---

**Готово!** Ваше Smurf приложение теперь работает онлайн с синхронизацией между устройствами! 🎉
